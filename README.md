# Production-Release-Incident-Management-Using-Git
PRODUCTION CHANGE, ROLLBACK &amp; RELEASE MANAGEMENT
---------------------------------------------------

🎯 What This Project Represents

This project simulates:
                   
                    Production deployment mistakes
                    Rollbacks
                    Selective fixes
                    Release versioning
--------------------------------------------------
Concepts Covered
        
        ✔ git revert (safe rollback)
        ✔ git reflog (recovery)
        ✔ git cherry-pick (selective fix)
        ✔ git rebase -i (clean history)
        ✔ Git tags (release management)
        ✔ Audit-friendly Git usage
--------------------------------------------------
